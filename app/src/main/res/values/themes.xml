<resources>

    <style name="Theme.Test" parent="@android:style/Theme.DeviceDefault.NoActionBar"/>

    <style name="Theme.Test.Transparent" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
    </style>

</resources>
